# Domain comes from LEGISNOTE_DOMAIN (infra/.env), injected by docker-compose.
# Set it to your real domain on the VPS and Caddy obtains + renews TLS
# automatically. Defaults to localhost (plain HTTP) for local runs.

{$LEGISNOTE_DOMAIN:localhost} {
	encode zstd gzip
	reverse_proxy web:3000
}
