# Copy to apps/web/.env for local development (outside Docker).
DATABASE_URL="postgres://legisnote:change-me@localhost:5432/legisnote"
AUTH_SECRET="generate-with-openssl-rand-base64-32"

# Shared secret the Python ingestion importer must present (POST /api/import).
IMPORTER_TOKEN="change-me-too"
